// var cheerio = require('cheerio');
// var pg = require('pg');
// var connectionString = process.env.DATABASE_URL || 'postgresql://localhost:5432/foodbot';

// exports.scrapeIngredients = function(html) {
//     var $ = cheerio.load(html);
//     var ingredientsTag = $('li.ingredients span.amount').children();
//     console.log('ingredients tag:', ingredientsTag);


// };
